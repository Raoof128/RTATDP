{{- if .Values.inferenceServer.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "threat-detector.fullname" . }}
  labels:
    {{- include "threat-detector.labels" . | nindent 4 }}
  {{- with .Values.inferenceServer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.inferenceServer.ingress.className }}
  ingressClassName: {{ .Values.inferenceServer.ingress.className }}
  {{- end }}
  {{- if .Values.inferenceServer.ingress.tls }}
  tls:
    {{- range .Values.inferenceServer.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.inferenceServer.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "threat-detector.fullname" $ }}
                port:
                  number: {{ $.Values.inferenceServer.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
